{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Window controls used only by the pet and settings windows",
  "windows": ["main", "settings"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Window controls used only by the pet window",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}

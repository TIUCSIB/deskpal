{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared capability for the desktop pet windows",
  "windows": ["main", "chat", "info", "settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
